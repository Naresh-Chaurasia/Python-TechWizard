= MCP VS Code Integration
:toc: right
:toclevels: 5
:sectnums: 5
:sectnumlevels: 5

== Integration with VS Code

* [x] Visual Studio Code
* [x] Continue Extension
* [ ] #Continue CLI#
** Think of it like this:
*** Your FastMCP server = Tool provider (exposes get_weather)
*** LLM (GPT) = Brain (decides when to use the tool)
*** Continue = Connector / Controller
*** Continue = AI agent runtime inside VS Code that enables LLMs to use your MCP tools.
* [x] OpenAI models
* [x] MCP (Model Context Protocol) server
* [x] Python MCP server executed using `uv`

This setup allows an AI model to call external tools (like a weather API) through MCP.

---


##################################################

---

* Install Required Tools (CLI)

Install the following tools.

* *Install Continue CLI*: `npm install -g @continuedev/cli``

* *Verify installation:*: `cn --help`

* *Install VC Code Continue Extension* After installing, restart VS Code.

---

=== Continue Plugin for VS Code

NOTE: Using config.yaml

Install the Continue plugin for VS Code from the marketplace, and verify that ~/.continue directory exists, and that it contains the expected files.

`ls ~/.continue`

----
~/.continue
config.ts
config.yaml
tsconfig.json
package.json
----

##################################################

---

=== MCP Server/GitHub API 

----
MCP-Henry-Habib/My-Code/API/github-api-private.py
----

##################################################

=== Continue Configuration for VS Code

TypeScript configuration file: `/.continue/config.ts`

Example configuration:

[source, typescript]
----
export function modifyConfig(config: any): any {
  return config;
}
----

##################################################

---

TypeScript configuration file: `/.continue/config.yaml`

Example configuration:

[source, yaml]
----
name: Local Config
version: 1.0.0

models:
  - name: GPT-4o-mini
    provider: openai
    model: gpt-4o-mini
    apiKey: "sk-proj-"

mcpServers:
  - name: github-private
    command: uv
    args:
      - run
      - --directory
      - /Users/nareshchaurasia/nc/Python-TechHub/AI-ML-GenAI/MCP/MCP-Henry-Habib/My-Code/API
      - mcp
      - run
      - "github-api-private.py"
    env:
      GITHUB_TOKEN: "ghp_"
----


##################################################

=== Test in VS Code

* *Restart VS Code*: After updating configuration, quit VS Code completely and restart it.

* *Test in VS Code*: Open Continue chat using `Cmd + L`

*Example prompt*: `Fetch Repositories from GitHub using the github-private MCP server`

##################################################

== What is `continue`


.Continue Plugin
[%collapsible]
====
*Continue* is an AI coding assistant for developers that runs inside editors like Visual Studio Code and in the terminal.

* It connects **LLMs (like GPT, Claude, etc.)** with your **codebase and tools**.
* It lets you **chat with AI about your code**, generate code, refactor, or debug.
* It supports **tool calling using MCP (Model Context Protocol)** so AI can execute external tools (APIs, databases, scripts).
* It works with multiple models (OpenAI, Anthropic, local models, etc.).
* It also provides a **CLI (`cn`)** to run AI workflows from the terminal.

In simple terms:

*Continue = AI layer that connects your editor + LLM + tools.*

Typical flow:

```
Editor / Terminal
      ↓
Continue
      ↓
LLM (GPT / Claude)
      ↓
Tools via MCP
      ↓
Your code / APIs / databases
```

So Continue acts like a **bridge between your development environment and AI models**.
====






